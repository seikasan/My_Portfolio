---
import styles from "./Hero.module.css";

interface Props {
  name: string;
  roleLine: string;
  summary: string;
  techStack: readonly string[];
  primaryHref: string;
  musicHref: string;
}

const { name, roleLine, summary, techStack, primaryHref, musicHref } = Astro.props as Props;
---

<section class={styles.hero} data-reveal>
  <div class={styles.copy}>
    <p class={styles.kicker}>Portfolio for Game Production</p>
    <p class={styles.name}>{name}</p>
    <h1>{roleLine}</h1>
    <p class={styles.summary}>{summary}</p>
    <ul class={styles.techList} aria-label="主要技術">
      {techStack.map((item) => (
        <li>{item}</li>
      ))}
    </ul>
    <div class={styles.actions}>
      <a class="button button--primary" href={primaryHref}>作品を見る</a>
    </div>
    <a class={styles.musicLink} href={musicHref}>音楽作品ページを見る</a>
  </div>
</section>
